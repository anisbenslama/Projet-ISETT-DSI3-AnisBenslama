<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Accueil</title>
</head>

<style>
  div {
    border: 1px solid black;
    margin-bottom: 10px;
    padding: 5px;
  }

  .container_bb {
    border-radius: 25px;
    padding: 20px;
    background: blanchedalmond;
    box-shadow: 15px 15px 30px #bebebe,
      -15px -15px 30px #ffffff;
    transition: 0.2s ease-in-out;
  }

  .container_bb:hover {
    cursor: pointer;
    box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.1);
  }

  strong {
    font-size: 25px;
  }

  p {
    font-size: 20px;
    color: rgb(39, 39, 39)
  }
</style>

<body>
  {{#if actualites.length}}
  <ul>
    {{#each actualites}}
    <div class="container_bb">
      <li>
        <strong>{{ titre }}</strong>
        <p>{{ description }}</p>
        <p style="text-decoration: underline;">date: {{ date_publication }}</p>
      </li>
    </div>
    <br>
    {{/each}}
  </ul>
  {{else}}
  <p>No news available.</p>
  {{/if}}
</body>

</html>